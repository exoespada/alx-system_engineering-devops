0-alias will create an alias ls for rm *
1-hello_you will print hello user where user is the current linux user
2-path will add /action as the last directory to PATH
3-paths with count the number of directories in PATH
4-global_variables will list the global variable
